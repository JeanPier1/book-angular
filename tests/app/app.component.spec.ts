import { describe, it, expect } from 'vitest';
import { TestBed } from '@angular/core/testing';

describe('AppComponent', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });

  it('should have TestBed available', () => {
    expect(TestBed).toBeDefined();
  });
});
